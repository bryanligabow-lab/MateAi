import type { Metadata } from "next";import { siteConfig } from "@/config/site";export const metadata:Metadata={title:"Contact",description:"Contact MateAI sales, support, or general inquiries."};export default function Contact(){const contacts=[["Sales",siteConfig.salesEmail,"Plans, demos, and implementation"],["Support",siteConfig.supportEmail,"Help with an existing MateAI service"],["General inquiries",siteConfig.contactEmail,"Partnerships and everything else"]];return <><section className="subhero centered"><span className="section-kicker">Contact</span><h1>Talk to the right team.</h1><p>Every address is configured centrally and can be changed without editing components.</p></section><section className="section contact-grid">{contacts.map(c=><a href={`mailto:${c[1]}`} key={c[0]}><span>↗</span><h2>{c[0]}</h2><p>{c[2]}</p><strong>{c[1]}</strong></a>)}</section></>}
