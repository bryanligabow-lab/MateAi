import type { MetadataRoute } from "next";import { industries } from "@/config/industries";import { siteConfig } from "@/config/site";export default function sitemap():MetadataRoute.Sitemap{const pages=["","/industries","/demo","/pricing","/book-demo","/contact","/about","/terms","/privacy","/cancellation","/ai-disclosure"];return [...pages,...industries.map(i=>`/${i.key}`)].map(path=>({url:`${siteConfig.url}${path}`,lastModified:new Date(),changeFrequency:path===""?"weekly":"monthly",priority:path===""?1:.7}))}
